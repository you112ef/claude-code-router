# 🚀 تعليمات بناء واستخدام تطبيق Claude Code Router Android

## ⚡ التحقق من بناء APK على GitHub

### 📋 الخطوات للتحقق من نجاح البناء:

1. **زيارة Pull Request**
   - اذهب إلى: https://github.com/you112ef/claude-code-router/pull/1
   - تحقق من حالة GitHub Actions (يجب أن تظهر ✅ خضراء)

2. **تحميل APK**
   - بعد نجاح البناء، اذهب إلى تبويب "Actions"
   - ابحث عن أحدث workflow run
   - قم بتحميل APK من قسم "Artifacts"

3. **اختبار التطبيق**
   - قم بتثبيت APK على جهاز Android (الحد الأدنى: Android 7.0)
   - افتح التطبيق وتحقق من الميزات التالية:

## 📱 ميزات التطبيق المدمجة

### 🏠 الشاشة الرئيسية (Home)
- **بدء الخدمة**: تشغيل خادم Node.js المحلي
- **إيقاف الخدمة**: إيقاف الخادم
- **عرض الحالة**: معلومات الخدمة (المنفذ، الحالة، وقت التشغيل)
- **معلومات الجهاز**: ذاكرة متاحة، معلومات الشبكة

### ⚙️ شاشة الإعدادات (Config)
- **إدارة مقدمي الخدمة**: OpenAI, Anthropic, Google, etc.
- **إعدادات الخادم**: المنفذ، المهلة الزمنية
- **إعدادات الأمان**: مفاتيح API، التشفير
- **استيراد/تصدير الإعدادات**: حفظ واستعادة التكوين

### 🧠 الذكاء الاصطناعي المحلي (Local AI)
- **تحميل النماذج**: دعم TensorFlow Lite, ONNX, GGML
- **إدارة النماذج**: حذف، تبديل، معلومات النموذج
- **معلومات الجهاز**: ذاكرة GPU، دعم التسريع
- **اختبار النماذج**: تجربة التوليد المحلي

### 🔌 الإضافات (Plugins)
- **تفعيل/إلغاء تفعيل**: إدارة الإضافات المختلفة
- **إعدادات الإضافة**: تخصيص كل إضافة
- **إضافة جديدة**: رفع ملفات إضافات مخصصة

### 📊 السجلات (Logs)
- **مراقبة الطلبات**: عرض جميع طلبات API
- **تصفية السجلات**: حسب النوع، الوقت، المصدر
- **تصدير السجلات**: حفظ لملف أو مشاركة
- **إحصائيات**: معدل الطلبات، أوقات الاستجابة

## 🛠️ التطوير المحلي

### متطلبات البناء:
```bash
# تثبيت المتطلبات
npm install -g @react-native-community/cli
cd claude-code-router-android
npm install

# بناء مشروع Node.js
node scripts/build-nodejs.js

# تشغيل على Android
npx react-native run-android

# أو بناء APK
cd android
./gradlew assembleRelease
```

### اختبار التطبيق:
```bash
# تشغيل الاختبارات
npm test

# فحص الكود
npm run lint
npm run typecheck

# التحقق الشامل
npm run validate
```

## 📦 بناء APK يدوياً

```bash
# في مجلد claude-code-router-android
cd android

# بناء APK تجريبي
./gradlew assembleDebug

# بناء APK للإنتاج
./gradlew assembleRelease

# APK سيكون في:
# android/app/build/outputs/apk/release/app-release.apk
```

## 🔒 أمان التطبيق

### الحماية المدمجة:
- **تشفير البيانات المحلية**: جميع الإعدادات مشفرة
- **التحقق من التواقيع**: فحص سلامة APK
- **حماية مفاتيح API**: تخزين آمن للمفاتيح
- **شبكة آمنة**: HTTPS فقط للاتصالات الخارجية

### التحقق من الأمان:
```bash
# فحص الثغرات الأمنية
npm audit

# التحقق من التوقيع
apksigner verify --verbose app-release-signed.apk
```

## 🌐 العمل بدون إنترنت

### النماذج المحلية المدعومة:
- **Llama 2/3**: نماذج GGUF للمحادثة
- **CodeLlama**: نماذج خاصة بالبرمجة  
- **TinyLlama**: نماذج خفيفة للأجهزة المحدودة
- **Phi-3**: نماذج Microsoft المحسنة

### استخدام النماذج المحلية:
1. حمل نموذج من قسم "Local AI"
2. اختر النموذج من القائمة
3. فعل وضع "Local Only" في الإعدادات
4. استخدم التطبيق بدون إنترنت

## ❓ حل المشاكل الشائعة

### مشكلة عدم بدء الخدمة:
- تحقق من الصلاحيات
- أعد تشغيل التطبيق
- تحقق من توفر المنفذ

### مشكلة النماذج المحلية:
- تأكد من وجود مساحة كافية
- تحقق من دعم الجهاز للتسريع
- جرب نموذج أصغر

### مشكلة الاتصال:
- تحقق من إعدادات الشبكة
- تأكد من صحة مفاتيح API
- فحص جدار الحماية

## 📧 الدعم والمساعدة

- **GitHub Issues**: https://github.com/you112ef/claude-code-router/issues
- **الوثائق**: راجع README.md في المشروع
- **المجتمع**: ناقش في Discussions

---

✨ **تم تطوير هذا التطبيق بواسطة Scout AI مع دعم كامل للعمل بدون إنترنت وواجهة مستخدم متقدمة!**